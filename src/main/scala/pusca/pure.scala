package pusca

/**
 * Marks the code (class/method) as pure (side-effect free).
 * Usually there's no need to use this annotation since it gets added to everything not marked @impure by the compiler.
 */
class pure extends StaticAnnotation
